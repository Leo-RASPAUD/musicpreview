export default theme => ({
    iconWrapper: {
        display: 'flex',
        alignItems: 'center',
        color: theme.palette.primary.main,
    },
    root: {
        marginTop: theme.spacing.unit,
    },
});
